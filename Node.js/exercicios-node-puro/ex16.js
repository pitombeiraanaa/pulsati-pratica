//crie um programa que leia a string de um usuário e as exiba em ordem alfabetica

let palavra;

const readline = require('readline').createInterface({
    input: process.stdin,
    output: process.stdout
  });

  readline.question('Digite a primeira palavra ', palavra1 => {
  });