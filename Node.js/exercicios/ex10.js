// crie um programa que leia o número do usuário e exiba o seu fatorial

let num;
count = 1;



const readline = require("readline").createInterface({
  input: process.stdin,
  output: process.stdout,
});

readline.question("Digite um número", (num) => {
 
 })
 