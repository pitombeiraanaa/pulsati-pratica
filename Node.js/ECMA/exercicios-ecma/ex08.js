// crie um programa que use as novas contantes do ECMAScript6 para definir uma constante que 
//represente uma conversão de temperatura (celsius para Fahrenheit)
const celsius = 20;
const fahrenheit = (celsius * 9) / 5 + 32;
console.log(`A temperatura de ${celsius}°C corresponde a ${fahrenheit}°F`);