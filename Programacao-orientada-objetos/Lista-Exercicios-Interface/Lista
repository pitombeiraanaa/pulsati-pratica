
1- Crie uma classe pessoa que possua os atributos nome e idade. 
Adicione um método que retorne uma string no formato "Nome: [nome], Idade: [idade]".

2- Crie uma classe estudante que herde da classe pessoa e adicione um 
atributoidentificadorEstudante. Adicione um método que retorne uma string no formato 
"Nome: [nome], Idade: [idade], Matrícula: [identificadorEstudante]".


3- Crie uma interface veiculo com métodos para acelerar e frear.

4- Crie três classes que implementem a interface veiculo: Carro, Bicicleta, Caminhao.


5- Adicione um atributo Velocidade à interface veiculo e implemente-o nas três classes criadas no exercício anterior.

6- Criar uma interface chamada "Calculadora" com dois métodos abstratos "somar" e "subtrair". 
Implemente a interface em uma classe chamada "CalculadoraImpl" e forneça implementações para os métodos "somar" e "subtrair".


7- Criar uma interface chamada "Veículo" com três métodos abstratos "acelerar", "frear" e "mudarMarcha". 
Implemente a interface em uma classe "Carro" e forneça implementações para os métodos "acelerar", "frear" e "mudarMarcha".


8- Criar uma interface chamada "MusicPlayer" com três métodos abstratos "play", "pause" e "stop".
 Implemente a interface em uma classe "MP3Player" e forneça implementações para os métodos "play", "pause" e "stop".



 9- Crie uma classe Carro com três variáveis membro: potência, velocidade e marca.
  Crie duas classes filhas chamadas Sedan e SUV que herdam os atributos da classe Carro. 
  Adicione um método acelerar() às duas classes filhas que simule o comportamento de aceleração 
  dos carros de acordo com suas respectivas potências.
  


  10- Crie uma interface Transporte que possui um método move(). 
  crie duas classes que implementam essa interface: Avião e Trem.
  Adicione um método para calcular o tempo de viagem para cada classe, usando
   diferentes algoritmos para calcular a velocidade média (para avião) e tempo de viagem (para trem).


 11- Crie ua classe abstrata chamada Reptil que herde da classe Animal e adicione um método abstrato
 para imprimir a temperatura corporal destes animais.
 Crie uma classe chamada Cobra que herde da classe Reptil e adicione um método para imprimir se a cobra é venenosa ou não.